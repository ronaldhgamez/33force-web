---
interface Props {
    title: string;
    width: string;
    height: string;
    alt: string;
    src: string;
    href: string;
}
const { title, width, height, alt, src, href } = Astro.props;
---


<address>
    <a class="link" href={href} target="_blank">
        <img src={src} alt={alt} width={width} height={height} />
        {title}
    </a>
</address>

  

<style>
    address {
        padding: 5px 20px;
        border-radius: 5px;
        transition: 0.5s ease;
    }

    address:hover {
        background: linear-gradient(to left, rgb(37, 96, 139, 0.1),rgb(92, 71, 97, 0.1));
        scale: 1.1;
    }

    .link {
        display: flex;
        gap: 12px;
        align-items: center;
        text-decoration: none;
        color: var(--text-color-light);
        font-style: normal;
        font-weight: bold;
        font-size: 1.0em;
    }
    
    @media screen and (max-width: 768px) {
        address {
            padding: 0px 10px;
        }
        .link {
            flex-direction: column;
            gap: 0;
            font-size: 0.8em;
            font-size: 0;
        }

        .link img {
            width: 35px;
            height: 35px;
        }
    }
</style>