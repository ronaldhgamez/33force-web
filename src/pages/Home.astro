<div class="home-container">
    <div class="image-container">
        <img src="home.webp" alt="Imagen de fondo de inicio, contrucción">
    </div>

    <div id="welcome" class="welcome" style="display: flex;">
        <h1 class="title">33FORCE</h1>
        <img class="triangle" style="transform: translate(0%, 4%)" src="favicon.svg" alt="33Force logo icon svg">
    </div>
    <p style="font-size: 1.3em; text-align: center;">En 33Force desarrollamos relaciones con nuestros clientes, basados en la integridad y el compromiso, desde el primer contacto hasta la finalización del proyecto.</p>
    <a class="button" href="" >Servicios</a>
</div>

<style>

    .home-container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        position: relative;
        gap: 15px;
        width: 100%;
        height: 100dvh;
        position: relative;

        outline: solid 3px blue;

        & p {
            max-width: 600px;
            color: var(--text-color-light);
        }
    }

    .image-container {
        position: absolute;
        z-index: -1;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
        background-color: var(--background-color);
        backdrop-filter: grayscale(10%);

        & img {
            aspect-ratio: 950 / 603;
            object-fit: cover;
        }
    }

    .title {
        font-size: 4em;
		background-image: var(--accent-gradient);
        filter: drop-shadow(2px 0px 0px rgba(255, 255, 255, 0.5));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}

    .triangle {
        filter: drop-shadow(0 8px 0px rgb(255, 255, 255, 0.2));
        transition: scale 0.7s ease, padding-left 0.5s ease;
    }

    .triangle:hover, .title:hover ~ .triangle {
        padding-left: 10px;
        scale: 1.6;
        filter: drop-shadow(0 0 30px white);
    }

    @media screen and (max-width: 840px) {
        .title {
            font-size: 3.3em;
        }

        .home-container {
            & p {
                font-size: 1.0em !important;
            }
        }
    }
</style>
